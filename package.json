{
  "name": "leetsquad-extension",
  "version": "1.0.0",
  "description": "Chrome extension to track friends' LeetCode progress and compete on leaderboards",
  "private": true,
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
